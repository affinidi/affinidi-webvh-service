// Re-export from webvh-common shared server infrastructure (placeholder)
// DIDComm messaging for the control plane follows the same pattern as
// webvh-server/src/messaging.rs. For the initial release, the control plane
// operates REST-only; DIDComm can be added later.
